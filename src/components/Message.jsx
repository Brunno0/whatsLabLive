import { MessageBox, MessageContainer } from './StyledMessage';


function Message() {
   
    return (
        <MessageContainer> 
            <MessageBox>
                 Mensagem
            </MessageBox>
        </MessageContainer>
    );
}

export default Message;
